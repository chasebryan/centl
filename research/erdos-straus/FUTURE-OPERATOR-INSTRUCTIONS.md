# Future Operator Instructions

**Date:** 2026-08-15  
**Scope:** `research/erdos-straus/`

## Standing order

Close admissible C2-shared without a `k` cap, then peel every shared core to the 3/5/7-adic cluster, then DSC-P. Keep Erdős-Straus claim boundaries honest.

## Priority

1. **Prove** that an admissible hard `r ≡ t (mod 4k−1)` cannot realise a complementary `q=3` cover. The finite statement is already exact through `k ≤ 1500` (`CN-SHARED-THEOREM.md`).
2. Formalise the lift-room peel for an arbitrary finite active core (roomy layers extend; only the tight cluster remains).
3. Bound `|N^{act} ∩ tight|` on Class-C residuals.
4. Do not revive unrestricted C2-shared: complementary covers exist for non-admissible `r`.

## Proved assets to use

- Totient-ratio: `φ(q)/φ(d) ≥ 2` for odd `q > d`
- Lift-room and uniform lift-room (`2τ(j) < φ(q)/φ(d)`)
- C2-thin: only `q=3` complementary can fail when `|R| ≤ 1`
- Admissible complementary search via CRT of trap-aligned `r`
- Thinness: `|R| ≤ δ_a + δ_b`
- Quotient rigidity parent files

## Forbidden

Announcing Erdős-Straus solved without a complete deposited proof.
Claiming unrestricted C2-shared after the complementary-cover counterexamples.
