# Future Operator Instructions

**Date:** 2026-08-15  
**Scope:** `research/erdos-straus/`

## Standing order

Extend C1 escape from `j ≤ 1500` to all `j`, then bounded `|N^{act}|`, then DSC-P. Keep Erdős-Straus claim boundaries honest.

## Priority

1. **Universalize C1** for `j > 1500` with `φ(q) ≤ |T_j|` using thinness + two-box geometry.
2. Promote full `C1-THEOREM` without the `j ≤ 1500` cap.
3. `|N^{act}| = 2` systems.
4. Mixed-box support-2.
5. Ancestry rigidity for further prime `s`.

## Proved assets to use

- Thinness: `|R| ≤ δ_a + δ_b` (two divisor classes mod `g`)
- Pigeonhole: `φ(q) > |T_j|`
- Program-range non-cover certificate `j ≤ 1500`
- Quotient rigidity parent files
- Multiplicative two-box representation

## Forbidden

Announcing Erdős-Straus solved without a complete deposited proof.
