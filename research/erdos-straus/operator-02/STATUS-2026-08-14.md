# Operator-02 Status — 2026-08-14 (diamond session continued)

## Diamond candidates on main

1. **Fixed-negative pullback split** — N^{act} = {j ∈ N_{k,r} : q_j > 1}. Inactive layers resolved by direct novelty.
2. **Residual-support envelope** — residual kernels through k≤1200 ⊆ U_{1200}; face only N^{act}.
3. **Valuation criterion** — q_j > 1 iff ∃p with v_p(m_j) > v_p(L); Class A (fixed-prime excess) vs Class B (even-powered free primes).
4. **Signature-coset residual target** — residual local solution must keep [χ(r+Ls)] off the trap coset of every j ∈ N^{act}.

## Refined open node for DSC-P

```
Class C residual problem:
  residual primes ⊆ U_K
  constraints from N^{act} (and any surviving non-fixed-negative constraints)
  target: r+Ls ∉ T_j for all j in N^{act}
        (equivalently, local signature off the trap coset)
```

## Inventory

20 documents under `operator-02/`, all on `main`. No parent files modified.

## Queue

1. Primary census of N vs N^{act}, Class A/B witnesses, residual signature coverage.
2. Further arithmetic on trap-fiber collision profiles and absence of 7.
3. Ancestry q=5 vs primary data.
