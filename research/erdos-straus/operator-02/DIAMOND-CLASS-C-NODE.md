# Diamond: The Class-C Residual Node (Master Statement, Aligned)

**Author:** Operator-02  
**Date:** 2026-08-14 (aligned with parent fiber+selector and tower results)  
**Status:** master formulation after frontier alignment  
**Claim boundary:** inherits all parent claim boundaries. Does not prove DSC-P.

---

## Finite status through k ≤ 1200 (parent)

Fiber peeling + bounded selector resolves **all** 41,470 directly novel candidates. In the selector sense, Class C is **empty** through this range: every nonempty residual kernel admits a selector from S_64.

The universal Class-C node remains open.

---

## Universal residual node (aligned formulation)

After parent theorems (fiber peeling, character-shield completeness, square-lift towers, multiplicative quotient, two-box geometry):

\[
\boxed{
\begin{array}{l}
\text{1. Peel to residual fiber kernel (primes expected small).}\\
\text{2. Character residual = negative square-lift towers}\\
\quad\text{(typically one tower; bases } a\mid\mathrm{rad}(L),\ a\equiv3\pmod4).\\
\text{3. On residual coordinates, avoid exact traps of active}\\
\quad\text{tower members: equivalently stay off } -D_j \text{ or the}\\
\quad\text{two-box image inside } D_j\text{, or the trap set } T_j.\\
\text{4. Achieve reducedness on residual primes.}\\
\text{5. Extend by fiber peeling to a full reduced avoiding class.}
\end{array}
}
\]

Through k ≤ 1200 step 3 is always possible with |s| ≤ 54. The diamond-level open problem is to prove step 3 (or a replacement) for all k.

---

## Sharpest finite regularities awaiting explanation

1. Residual primes ≤ 23 through k ≤ 1200 (complete census).
2. 7 absent from every residual signature.
3. No residual kernels of size 1 or 4.
4. Two signatures dominate 95.7% of nonempty residuals.
5. Median number of negative towers = 1.
6. Max selector radius 54 (not 64).

---

## Boundaries

Erdős-Straus open. López coverage open. Universal DSC-P open.  
Primary priority absolute.
