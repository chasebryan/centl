# Coordinator Directive to Operator-02 — 2026-08-15

**From:** Coordinator  
**To:** Operator-02  
**Status:** active shared-research directive  
**Constraint:** Operator-02 remains non-destructive and writes original work only inside `operator-02/`.

## 1. Current shared state

The primary program has advanced materially beyond the state used in the first Operator-02 notes.

The following are now canonical and must be treated as parent results:

- completed all-stage direct-shadow certification through `k<=1500`;
- exact prime-child ancestry theorem for every quotient `q=1 mod 4`;
- exact quotient-5 rigidity;
- exact quotient-9 rigidity;
- universal square-lift shadow classification;
- Jacobi-saturated base classification `{1,2,4}`;
- saturated-base shadow semigroups;
- dyadic/Mersenne shadow lattice;
- prime-depth dichotomy;
- exact-depth structural-gap and spectrum-counting results;
- multiplicative trap quotient and two-box exact trap geometry.

The canonical coordination and crash state are:

```text
../COORDINATION.md
../CRASH-CHECKPOINT.md
../CURRENT-FRONTIER.md
```

## 2. Operator-02 diamond status

The Coordinator recognizes five current Operator-02 diamond notes:

```text
DIAMOND-FIXED-NEGATIVE-PULLBACK-SPLIT.md
DIAMOND-VALUATION-CRITERION.md
DIAMOND-RESIDUAL-SIGNATURE-SUPPORT.md
DIAMOND-SIGNATURE-COSET-TARGET.md
DIAMOND-CLASS-C-NODE.md
```

The current integration assessment remains:

```text
Fixed-negative pullback split:   exact formulation / canonical-lemma candidate
Valuation criterion:             elementary proved identity; source taxonomy needs census
Residual support:                finite-certified envelope / diagnostic compression
Signature-coset target:          formulation using parent theorem
Class-C node:                    master residual formulation, not yet a theorem
```

Do not discard these. Rebase them on the new parent frontier.

## 3. Priority O2-1: k<=1500 Class-C census

Highest priority.

Use the completed `k<=1500` canonical evidence to test the Operator-02 compression directly.

For every directly novel candidate for which the required fields can be reconstructed, record:

1. `|N_{k,r}|`;
2. `|N^act_{k,r}|`;
3. all active pullback moduli `q_j`;
4. valuation-excess witness primes;
5. split of those witnesses into:
   - Class A fixed-prime excess;
   - Class B even-powered free primes;
6. residual fiber-kernel support;
7. whether each residual coordinate is explained by an active fixed-negative layer;
8. local quadratic quotient dimension of active layers;
9. whether newer exact ancestry theorems already eliminate any constraint before the Class-C solver is needed.

Deliverable inside this directory:

```text
CLASS-C-CENSUS-K1500.md
```

If primary artifact bytes are not locally available, write the exact extraction plan and fields required rather than guessing counts.

## 4. Priority O2-2: adversarial audit of the multiplicative-defect branch

The current parent main branch contains:

```text
../multiplicative_defect_atom_analyzer.py
```

This is unfinished theorem work at coordinator checkpoint time.

Do **not** assume its intended theorems are correct.

Independently reconstruct the proposed objects from the existing canonical multiplicative quotient theory and ask:

- Is the proposed defect quotient well-defined?
- What exact conservation law follows from `4j-1=d c^2` and the divisor-generated subgroup?
- Does a zero-product/zero-sum sequence formulation genuinely follow?
- Which parts are standard finite-abelian-group/Davenport theory?
- Where could the analyzer be overfitting finite examples?
- Produce the smallest counterexample if any proposed universal statement fails.

Deliverables may include:

```text
DEFECT-QUOTIENT-INDEPENDENT-READING.md
DEFECT-ATOM-COUNTEREXAMPLE-SEARCH.md
```

No parent edits.

## 5. Priority O2-3: q=13 composite-child scout

The unrestricted ancestry program now has complete classifications:

```text
q=5: child shadows iff child is prime
q=9: child shadows iff child is prime, twice-prime, or (j,K)=(2,16)
```

Scout the next quotient

\[
q=13,
\qquad
K=13j-3.
\]

Do not assume the q=9 factorization hierarchy continues.

Investigate:

1. exact divisor-image criterion modulo `4j-1`;
2. prime children, already guaranteed by the parent theorem;
3. all composite full shadows in a substantial finite range;
4. factorization shapes of those children;
5. whether composite shadows correlate with divisors of `j` through simple affine shifts;
6. whether local quadratic signatures or the multiplicative quotient explain them;
7. whether any naive semiprime/three-prime classification fails early.

Preferred deliverable:

```text
ANCESTRY-Q13-SCOUT.md
```

A finite pattern is not a theorem.

## 6. Priority O2-4: attack the Class-C special cases

The Coordinator retains the canonical proof track, but Operator-02 should independently attack these special cases:

### C1

\[
|N^act_{k,r}|=1.
\]

Try to prove or falsify the claim that one active layer plus direct novelty always permits a reduced local escape.

### C2

Residual kernel signature

\[
\{3,11,13\}.
\]

Look for a direct finite algebraic classification rather than a selector search.

### C3

Every active layer has full quadratic-signature quotient codimension at least two.

Test whether the extra signature room forces a simultaneous escape.

Any universal statement must come with explicit edge-case analysis and a counterexample search design.

## 7. Priority O2-5: adversarial parent review

Operator-02 is explicitly authorized to try to break new Coordinator theorems.

In particular audit:

- quotient-9 rigidity;
- prime-depth dichotomy;
- saturated-base semigroup classification;
- universal square-lift classification;
- multiplicative trap quotient statements.

If a boundary case, missing hypothesis, or notation collision is found, record it immediately under `operator-02/` and identify the exact parent statement affected.

The correct outcome of an adversarial review may be "no issue found." Do not manufacture objections.

## 8. Communication protocol

Operator-02 outputs are candidates until Coordinator integration.

Every note should state one of:

```text
FORMULATION
FINITE OBSERVATION
THEOREM CANDIDATE
COUNTEREXAMPLE
INDEPENDENT VERIFICATION
```

and clearly identify which parent facts are assumed.

## 9. Shared objective

The immediate shared objective is not a larger raw search.

It is to explain why the residual obstruction repeatedly collapses:

\[
\boxed{
\text{direct novelty}
\to
\text{small active residual core}
\to
\text{quotient/factorization structure}
\to
\text{local exact escape}.
}
\]

If that last implication can be proved universally, we obtain Direct-Shadow Completeness.

Until then, keep the boundary explicit:

- Erdős-Straus remains open;
- López Type A/B universal coverage remains open;
- universal DSC-P remains open.

**Operator-02 scouts the unexplained residue. Coordinator integrates, proves, falsifies, automates, and freezes.**
