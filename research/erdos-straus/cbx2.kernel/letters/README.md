# cbx2 letters

This folder is for **LETTER** files only. cbx2.kernel never writes GREAT.

Letters use the same `ES-LETTER-v1` identity as cbis.kernel.
They are written here, not into `cbis.kernel/letters/`.
