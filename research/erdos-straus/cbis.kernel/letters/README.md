# CBIS letters

This folder is for **LETTER** files only. cbis.kernel never writes GREAT.

Each letter is `L-<32 hex chars>.md`, the same `ES-LETTER-v1` number as
bb.kernel and cbap.kernel. Finding a letter by inverse covering does
not change the letter.

Erdős–Straus remains open.
