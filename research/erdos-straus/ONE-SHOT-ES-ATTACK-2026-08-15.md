# One-shot Erdős–Straus attack — live checkpoint

**Date:** 2026-08-15  
**Status:** active proof hunt / durable checkpoint  
**Claim boundary:** no claim of a proof until the final universal step is independently verified.

## 1. Canonical correction

The universal Direct-Shadow Completeness route is dead and must not be resurrected as the main ES bridge. `CURRENT-FRONTIER.md` records a hosted-verified directly-novel but union-shadowed three-row `q=3` counterexample. This falsifies DSC-0 and DSC-P but does **not** threaten Erdős–Straus, because a union-shadowed target candidate is already solved at an earlier Type A/B layer.

Therefore the one-shot target is the actual pointwise prime statement:

\[
\boxed{\text{every Mordell-hard prime has at least one Type A/B / fab certificate}.}
\]

Equivalently: every prime escaping the density-one prime-modulus backbone must receive a composite rescue.

## 2. Exact divisor criterion now used as primary language

For prime `p == 1 (mod 4)` and coprime `a,b<p`, `FAB-COPRIME-DIVISOR-CRITERION.md` proves

\[
\operatorname{fab}(p,a,b)>0
\iff
\exists k\mid(a+bp):\quad k\equiv-p\pmod{4ab}.
\]

This is a pure divisor-in-residue-class target.

## 3. Exact necessary restrictions on a hypothetical hard-prime counterexample

`FAB-HARD-FIRST-FILTERS.md` proves that any Mordell-hard prime counterexample must simultaneously satisfy:

1. every odd prime factor of `(p+1)/2` is `1 mod 4`;
2. every prime factor of `(p+3)/4` is `1 mod 3`;
3. every prime factor of `(3p+1)/4` is `1 mod 3`;
4. every prime factor of `p+2` is `1 or 3 mod 8`.

Writing

\[
A=(p+3)/4,
\qquad
B=(3p+1)/4=3A-2,
\]

both `A` and `B` must lie in the multiplicative semigroup generated by rational primes `1 mod 3`.

## 4. Nonresidue bridge

`FAB-HARD-NONRESIDUE-BRIDGE.md` proves that every coprime fab certificate for a Mordell-hard prime imports an odd external prime

\[
\ell\ge 11,
\qquad
(\ell/p)=-1.
\]

Thus the same small-prime boundary (`11,13,...`) reached by the earlier shadow/kernel program reappears from the divisor-parametrization direction.

## 5. Discarded one-shot idea: universal q=3 triangle absorption

A tempting route was to combine pointwise q=3 absorption with quotient-9 ancestry rigidity to prove that every q=3 singleton descends. This cannot be universal because the canonical repo now contains an independently verified three-row q=3 union-shadow core. The route is retained only as depth-spectrum structure, not as the ES bridge.

## 6. Active one-shot hypothesis

The current target is to exploit the **intersection** of the four shifted-factor restrictions rather than add many unrelated congruence families.

The desired theorem shape is:

> For every hard prime `p`, if the first four exact filters all fail to give a certificate, then one fixed additional coprime pair `(a,b)` (or one finite algebraically forced family) must satisfy the divisor criterion.

The strongest candidate should be selected by exact computation on the simultaneous-factorization survivor set, then proved from the survivor restrictions themselves.

## 7. Next exact checks

1. enumerate hard primes over a substantial range;
2. keep only primes satisfying all four necessary counterexample restrictions;
3. for small coprime `(a,b)`, test the exact divisor criterion on these survivors;
4. identify whether one pair or a tiny family kills the entire survivor population;
5. inspect the factorization congruence responsible for the winning pair;
6. attempt to prove that factorization pattern universally from the four survivor restrictions;
7. certify any resulting Egyptian-fraction identity in CENTL form.

This file is intentionally updated as the proof hunt advances so a chat/runtime failure does not destroy the research state.
