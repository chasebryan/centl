# Exact intersections of k27 phase selectors

**Status:** exact cross-coordinate elimination module inside the candidate decomposition framework  
**Date:** 2026-08-16  
**Verifier:** `verify_k27_phase_intersection_elimination.py`  
**Depends on:** `K27-LATER-PHASE-FEEDBACK.md`, `K27-QR-FACTOR-MODE-SELECTORS.md`, and the realized k19/k31 survivor-mode theorems.  
**Claim boundary:** exact necessary mode intersections and contradictions inside the k27 survivor grammar. This is not a termination theorem, not a closed decomposition method, and not an Erdős–Straus proof.

## 1. Phase selectors are set-valued constraints

The landed feedback theorem gives the following exact k27 nonresidue-skeleton mode sets on a k27 miss:

```text
tau13=8   -> {Q,E}
tau17=6   -> {B,D}
tau19=8   -> {Q}
tau31=7   -> {Q,A,D}
tau43=27  -> {Q}.
```

Because all of these statements concern the same exact k27 NR-skeleton coordinate, simultaneous phase conditions require the **intersection** of the corresponding mode sets.

An empty intersection is an exact contradiction with k27 survival.

A singleton intersection is a mode selector.

This is the first place where independent CRT phase coordinates can resolve or eliminate a k27 state without any factorization of E.

## 2. Complete pairwise intersection table

For the five nontrivial phase selectors above:

```text
phase pair                     intersection      consequence
----------------------------------------------------------------
tau13=8  + tau17=6             empty             k27 hit
tau13=8  + tau19=8             {Q}               Q
tau13=8  + tau31=7             {Q}               Q
tau13=8  + tau43=27            {Q}               Q

tau17=6  + tau19=8             empty             k27 hit
tau17=6  + tau31=7             {D}               D
tau17=6  + tau43=27            empty             k27 hit

tau19=8  + tau31=7             {Q}               Q
tau19=8  + tau43=27            {Q}               Q

tau31=7  + tau43=27            {Q}               Q.
```

The `tau17=6 + tau31=7 -> D` row is especially important: it is the first pure CRT-phase combination that selects a **unique non-Q k27 mode**.

## 3. Exact contradiction progressions

The empty intersections correspond to exact CRT classes.

### tau13=8 and tau17=6

CRT gives

`t = 125 mod221`.

On this progression rational primes13 and17 both divide E. Their exact k27 requirements are respectively

`{Q,E}`

and

`{B,D}`,

which are disjoint.

Therefore

`t=125 mod221 -> k27 hits`.

### tau17=6 and tau43=27

CRT gives

`t = 329 mod731`.

The q17 factor requires `{B,D}`, while q43 is a Q selector.

Therefore

`t=329 mod731 -> k27 hits`.

### tau19=8 and tau17=6

CRT gives

`t = 93 mod323`.

The q19 factor forces Q, while q17 requires `{B,D}`.

Therefore

`t=93 mod323 -> k27 hits`.

These are exact periodic k27 absorption classes generated by **factor incompatibility**, not by an individual one-factor killer.

## 4. Exact non-Q selector progression

The phase pair

```text
tau17=6
tau31=7
```

has CRT solution

`t = 346 mod527`.

The q17 factor restricts the k27 NR skeleton to `{B,D}`.

The q31 factor restricts it to `{Q,A,D}`.

Their intersection is exactly

`{D}`.

Hence

`t=346 mod527 AND k27 miss -> k27_NR_mode=D`.

This is a range-free phase-to-mode theorem.

## 5. Additional Q selector progressions

The remaining nonempty pair intersections that sharpen the mode to Q include:

```text
tau13=8 + tau31=7   -> t=255 mod403  -> Q
tau13=8 + tau43=27  -> t=177 mod559  -> Q
tau31=7 + tau43=27  -> t=1316 mod1333 -> Q.
```

Pairs involving tau19=8 or tau43=27 are Q whenever they are not contradictory because each of those individual phases already forces Q.

## 6. Route-B BARE loses a k51-survivor phase

The realized Route-B k19 BARE state forces

`tau19=8`.

Therefore a BARE branch that also has

`tau17=6`

lands in the exact contradiction

`{Q} intersection {B,D} = empty`.

Hence

`Route-B k19 BARE + tau17=6 -> k27 hits`.

The standalone k51 phase theorem allows tau17=6. The contradiction appears only after the upstream BARE/k27 dependency is propagated.

In the Route-B ancestry coordinate

`t = 705 + 1081u`,

`tau17=6` is equivalent to

`u=10 mod17`.

Thus any Route-B BARE survivor through k27 must satisfy

`u != 10 mod17`.

Because Route-B BARE itself fixes `u=16 mod19`, the excluded joint class is

`u = 282 mod323`.

This is an ancestry-conditioned phase deletion generated entirely by exact mode intersection.

## 7. Route-B phase pair forces three local modes

On Route B, suppose

```text
tau17=6
tau31=7
```

and the branch survives k27 and k31.

The k27 phase intersection forces

`k27_NR_mode=D`.

The phase tau31=7 is outside the k31 BARE phase set `{0,19,29}`, so

`k31_mode=FULL_QR`.

Route-B k19 BARE would force tau19=8, which is incompatible with tau17=6 at k27. Therefore

`k19_mode=FULL_QR`.

So the exact branch-local implication is

```text
Route B
tau17=6
tau31=7
survive k19,k27,k31
    -> k19 FULL_QR
    -> k27 D
    -> k31 FULL_QR.
```

The phase pair is

`t=346 mod527`.

In Route-B ancestry it is equivalent to

`u=299 mod527`.

This is the first two-phase selector in the present grammar that forces **three local survivor-mode coordinates simultaneously**.

## 8. Constraint-engine consequence

The exact propagator can represent every phase selector as an allowed domain for `k27_NR_mode` and intersect those domains automatically.

Conceptually:

```text
K27_ALLOWED = {Q,A,B,C,D,E,F}

if tau13==8:  K27_ALLOWED &= {Q,E}
if tau17==6:  K27_ALLOWED &= {B,D}
if tau19==8:  K27_ALLOWED &= {Q}
if tau31==7:  K27_ALLOWED &= {Q,A,D}
if tau43==27: K27_ALLOWED &= {Q}

if K27_ALLOWED == empty:
    contradiction
```

This is exactly the kind of propagation that should occur before factor enumeration.

## 9. Bryan Entanglement Cross / BREC boundary

A phase collision that empties the k27 mode set is naturally interpretable as converging obstructive/excavation pressure; a singleton intersection exposes constructive structure.

That description is optional metadata. The CRT conditions and exact mode-set intersection are the proof-bearing objects.

## 10. Next target

Extend the executable dependency propagator to the later phase coordinates and compile these set intersections directly.

Once that is landed, the next mathematical target is higher-order collision structure: identify small phase combinations that force a terminal signed-box hit at k27, k31, or k35 even though every proper subcombination remains nonterminal.