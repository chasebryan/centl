# Proposed contribution packs

CENTL-SCi may stage a reviewed local pack here after `stage contribution`.

Nothing in this directory is verified CENTL core. A pack becomes upstream
only if a human opens a **draft** pull request against `mirage` and that
request is approved. `oasis` is never the automatic base.

Do not put tokens, home-directory paths, or private journal text here.
